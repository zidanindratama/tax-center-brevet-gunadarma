import React from "react";

type Props = {
  batchSlug: string;
  meetingId?: string;
};

function NilaiDatatable({ batchSlug, meetingId }: Props) {
  return <div>NilaiDatatable</div>;
}

export default NilaiDatatable;
